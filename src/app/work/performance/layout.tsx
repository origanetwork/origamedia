import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Performance & Direct Response Marketing | ORIGA Media",
    description: "Discover our high-ROI growth campaigns, meta advertising funnels, lead generation pipelines, and automated CRM setups.",
};

export default function WorkPerformanceLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
